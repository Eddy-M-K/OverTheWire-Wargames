#include <iostream>
#include <cassert>
#include "Problem-1.cpp"

std::string return_xor(const std::string &hex_one, const std::string &hex_two)
{
    assert(hex_one.length() == hex_two.length());

    std::string xor_string{};
    std::vector<uint8_t> bytes_one{}, bytes_two{};
}

int main()
{
    return 0;
}
